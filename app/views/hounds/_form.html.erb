<%= form_for(@hound) do |f| %>
  <% if @hound.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hound.errors.count, "error") %> prohibited this hound from being saved:</h2>

      <ul>
      <% @hound.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br />
    <%= f.text_field :age %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
